# cd

The cd (change directory) command in Linux is used to navigate between folders (directories).   

Key commands include cd [dir] to move to a directory, cd .. to move up, cd ~ or just cd to return to the home directory, and cd - to return to the previous directory. It requires execute permissions for the target folder. 


## example
`cd /home/username/Documents`
